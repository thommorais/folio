{
	"name": "dashboard",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "go run . serve",
		"typegen": "npx pocketbase-typegen --db ./pb_data/data.db --out ../web/src/pocketbase-types.ts",
		"seed": "go run . seed"
	},
	"dependencies": {
		"tailwind-variants": "^0.2.1"
	}
}
